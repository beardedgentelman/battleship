const Header = ({ headerTitle }) => {
  return (
    <header className='max-w-7xl mx-auto py-2 px-4'>
      <h1 className='text-5xl font-bold text-center w-full'>{headerTitle}</h1>
    </header>
  )
}

export default Header
